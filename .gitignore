node_modules/
dist/
build/
.DS_Store

# secrets — never commit
.env
.env.local
.env.*.local
server/.env

# uploads + generated worlds (fallbackWorld.json IS committed, deliberately)
uploads/
*.log
coverage/
.vite/

# demo recordings are large; keep them out of git
*.mov
*.mp4

# TypeScript incremental build cache — regenerated every build, causes conflicts
*.tsbuildinfo

# backup client build output
backup-client/dist/

# walkthrough recorder output
backup-client/walkthrough-out/
