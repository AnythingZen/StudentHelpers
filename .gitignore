node_modules/
dist/
build/
.DS_Store

# secrets — never commit
.env
.env.local
.env.*.local
server/.env

# uploads + generated worlds (fallbackWorld.json IS committed, deliberately)
uploads/
*.log
coverage/
.vite/

# demo recordings are large; keep them out of git
*.mov
*.mp4

# brain
server/brain/node_modules/
server/brain/.venv/

server/brain/.ocr-cache/
*.traineddata
