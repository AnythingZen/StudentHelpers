// The server's own pure rules, bundled into the client, so a grove the server
// considers locked is exactly the grove the client draws as locked.
export { conceptHealth, isLocked, UNLOCK_THRESHOLD } from '../../server/src/schedule';
