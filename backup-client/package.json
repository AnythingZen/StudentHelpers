{
  "name": "mastery-grove-backup-client",
  "private": true,
  "type": "module",
  "description": "Builder C's standalone student client — the team's backup forest. Separate from Builder A's client/ so the two never conflict.",
  "engines": { "node": ">=22.12" },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "0.186.0"
  },
  "devDependencies": {
    "@types/three": "0.186.0",
    "typescript": "5.9.3",
    "vite": "8.3.0"
  }
}
